# Frontend (eBayOS) Environment Variables
# ==========================================

# Node environment
NODE_ENV=production

# Server port
PORT=4873

# Backend API URL (points to Docker backend service or IP)
BACKEND_URL=http://ebay-listing-assistant:5000

# Gemini API key for NL command parsing (intent parser runs client-side)
GEMINI_API_KEY=YOUR_GEMINI_API_KEY_HERE

# HTTP fallback mode (0 = enforce HTTPS with Tailscale certs, 1 = allow HTTP)
USE_HTTP=0

# Tailscale HTTPS Certificate Paths (optional - only set in production)
# These are auto-renewed by the Tailscale daemon
# CERT_PATH=/etc/certs/tailscale/pop-os.tail2de5b8.ts.net.crt
# KEY_PATH=/etc/certs/tailscale/pop-os.tail2de5b8.ts.net.key
